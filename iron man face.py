"""
Python Turtle Crash Course - Iron Man Face Drawing

"""

import turtle

# ═══════════════════════════════════════════════════
# إحداثيات خوذة Iron Man - 3 أجزاء
# ═══════════════════════════════════════════════════

# الجزء العلوي (القناع العلوي)
piece1 = [
    [(-40, 120), (-70, 260), (-130, 230), (-170, 200), (-170, 100), 
     (-160, 40), (-170, 10), (-150, -10), (-140, 10), (-40, -20), (0, -20)],
    [(0, -20), (40, -20), (140, 10), (150, -10), (170, 10), (160, 40), 
     (170, 100), (170, 200), (130, 230), (70, 260), (40, 120), (0, 120)]
]

# الجزء الأوسط (الوجه)
piece2 = [
    [(-40, -30), (-50, -40), (-100, -46), (-130, -40), (-176, 0), 
     (-186, -30), (-186, -40), (-120, -170), (-110, -210), (-80, -230), 
     (-64, -210), (0, -210)],
    [(0, -210), (64, -210), (80, -230), (110, -210), (120, -170), 
     (186, -40), (186, -30), (176, 0), (130, -40), (100, -46), 
     (50, -40), (40, -30), (0, -30)]
]

# الجزء السفلي (الذقن)
piece3 = [
    [(-60, -220), (-80, -240), (-110, -220), (-120, -250), (-90, -280), 
     (-60, -260), (-30, -260), (-20, -250), (0, -250)],
    [(0, -250), (20, -250), (30, -260), (60, -260), (90, -280), 
     (120, -250), (110, -220), (80, -240), (60, -220), (0, -220)]
]

# ═══════════════════════════════════════════════════
# إعدادات الشاشة
# ═══════════════════════════════════════════════════
turtle.hideturtle()                    # إخفاء السلحفاة
turtle.bgcolor('#ba161e')              # خلفية أحمر داكن (لون Iron Man)
turtle.setup(700, 8ؤي00)                 # حجم النافذة 500×600 بكسل
turtle.title("انا الرجل الحديدي")           # عنوان النافذة

# نقاط البداية لكل جزء
piece1Goto = (0, 120)
piece2Goto = (0, -30)
piece3Goto = (0, -220)

turtle.speed(2)                        # سرعة الرسم

# ═══════════════════════════════════════════════════
# دالة رسم القطعة
# ═══════════════════════════════════════════════════
def draw_piece(piece, pieceGoto):
    """ترسم قطعة من خوذة Iron Man"""
    turtle.penup()                     # رفع القلم (لا رسم أثناء التنقل)
    turtle.goto(pieceGoto)             # الانتقال لنقطة البداية
    turtle.pendown()                   # إنزال القلم (بدء الرسم)
    turtle.color('#fab104')            # لون أصفر ذهبي
    turtle.begin_fill()                # بدء تعبئة اللون
    
    # رسم الجزء الأول من القطعة (النصف الأيسر)
    for i in range(len(piece[0])):
        x, y = piece[0][i]
        turtle.goto(x, y)
    
    # رسم الجزء الثاني من القطعة (النصف الأيمن)
    for i in range(len(piece[1])):
        x, y = piece[1][i]
        turtle.goto(x, y)
    
    turtle.end_fill()                  # إنهاء التعبئة

# ═══════════════════════════════════════════════════
# رسم الأجزاء الثلاثة
# ═══════════════════════════════════════════════════
draw_piece(piece1, piece1Goto)       # الجزء العلوي
draw_piece(piece2, piece2Goto)       # الجزء الأوسط
draw_piece(piece3, piece3Goto)       # الجزء السفلي

turtle.hideturtle()                    # إخفاء السلحفاة بعد الرسم
turtle.done()                          # إنهاء البرنامج وإبقاء النافذة مفتوحة